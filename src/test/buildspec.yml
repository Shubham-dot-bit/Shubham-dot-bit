version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11

  pre_build:
    commands:
      - pip install flask pytest

  build:
    commands:
      - pytest tests/

  post_build:
    commands:
      - echo "Build completed successfully"
